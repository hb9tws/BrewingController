<local:BindablePage
    x:Class="BrewingController.MultiRestMash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrewingController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding MultiRestMash, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" Margin="50">
            <TextBlock  Text="Multi-Rest Mash" FontSize="64" Foreground="{ThemeResource ApplicationHeaderForegroundThemeBrush}" />
            <Button Content="Back" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="83" FontSize="32" Command="{Binding BackCommand, Mode=OneWay }" />
        </StackPanel>
    </Grid>
</local:BindablePage>
